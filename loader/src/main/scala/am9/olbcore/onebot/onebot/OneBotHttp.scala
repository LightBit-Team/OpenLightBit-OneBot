package am9.olbcore.onebot.onebot

class OneBotHttp extends OneBot {
  //todo with todo
  override def sendGroup(groupId: Long, message: String): Unit = {
    ???
  }
  override def sendGroupWithCqCode(groupId: Long, message: String): Unit = {
    ???
  }
}
